[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short -n 8 -q
# Параллельно на 8 воркеров (нужен pytest-xdist): pytest -n 8
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
